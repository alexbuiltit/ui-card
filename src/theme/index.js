import { typography } from "./typography";
import { layout, baseSize } from "./layout";
import { colors } from "./colors";

const theme = { baseSize, typography, layout, colors };

export default theme;
